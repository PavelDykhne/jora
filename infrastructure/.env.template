# ============================================================
# OpenClaw Job Hunter Agent — Environment Variables
# ============================================================
# Copy: cp .env.template .env && nano .env
# NEVER commit .env to git!
# ============================================================

# Telegram Bot (from @BotFather)
TG_BOT_TOKEN=
TG_CHAT_ID=

# Anthropic API
ANTHROPIC_API_KEY=

# Enrichment tuning (defaults are fine)
DUPLICATE_THRESHOLD=0.85
RELEVANCE_SCORE_MIN=0
POLL_INTERVAL_MS=60000
